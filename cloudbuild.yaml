# cloudbuild.yaml
steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  args:
    - gcloud
    - functions
    - deploy
    - imginfo-checkimg-v2 # ユニークなFunction名
    - --source=.
    - --runtime=python311
    - --trigger-http
    - --allow-unauthenticated
    - --entry-point=makemodel # main.pyの関数名
    - --region=asia-northeast1
    - --set-env-vars=GCS_BUCKET_NAME=model-raw-image # GCSのバケット名
# 以下を追加
options:
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET